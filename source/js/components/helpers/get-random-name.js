import getRandomArrayItem from "./get-random-array-item";

function getRandomName(dictionary) {
  return getRandomArrayItem(dictionary);
  // dictionary.map((item) => {
  //   return item
  // })
}

export default getRandomName;
